<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Potify" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:textColor">@color/white</item>
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
    </style>

    <style name="Theme.Potify" parent="Base.Theme.Potify" />
</resources>